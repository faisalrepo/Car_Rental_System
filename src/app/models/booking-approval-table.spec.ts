import { BookingApprovalTable } from './booking-approval-table';

describe('BookingApprovalTable', () => {
  it('should create an instance', () => {
    expect(new BookingApprovalTable()).toBeTruthy();
  });
});
