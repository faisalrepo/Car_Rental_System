import { CarTransactionTable } from './car-transaction-table';

describe('CarTransactionTable', () => {
  it('should create an instance', () => {
    expect(new CarTransactionTable()).toBeTruthy();
  });
});
